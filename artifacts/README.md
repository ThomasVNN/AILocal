# Artifacts Area

- `artifacts/images/`: tar image và snapshot data đã export.
- `artifacts/backups/`: backup lịch sử/manual snapshots.

Khu vực này không thuộc deploy path chuẩn của stack 2-layer hiện tại.
